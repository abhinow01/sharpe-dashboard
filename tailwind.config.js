// tailwind.config.js
// tailwind.config.cjs

export default {
  content: ['./index.html', './src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {
      // Add custom theme configurations or extend the default theme here
    },
  },
  plugins: [
    // Add custom plugins or use built-in Tailwind plugins here
  ],
}
